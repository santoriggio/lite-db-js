pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      run: yarn eslint {staged_files}
    typecheck:
      glob: "*.{ts,tsx}"
      run: yarn tsc {staged_files} --noEmit

commit-msg:
  commmands:
    lint-msg:
      run: yarn commitlint --edit
